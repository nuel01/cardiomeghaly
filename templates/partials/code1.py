# import os
# import sys
# from sklearn.externals import joblib
# loaded_model = joblib.load('piped.pkl')
# loaded_text='cardiac silhouette is normal in size and contour. CTR of 0.47'

# print(loaded_model.predict(loaded_text))
